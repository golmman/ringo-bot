const { expect } = require('chai');
const { isBlueWinAt, isRedWinAt } = require('../src/evaluation');

describe('evaluation', () => {
    it('blue wins', () => {
        expect(1).to.equal(1);
    });
});
